<div id='chat-wrapper'>
    <div id="position-wrapper-chat">
        <div class='chat-messages' data-action="turbolinks:before-visit@window->chat#saveScrollPosition" data-target='chat.messages'>
            <%= render partial: 'messages/message', collection: messages, as: :message %>
        </div>
        <div class='back-scroll'>
            <span class='btn-scroll-down' 
                data-target="chat.scrollDown"
                data-action="click->chat#scrollToDown">
            ↓
            </span>
        </div>
        <div class='chat-message-form'>
            <%= render partial: 'messages/form', locals: { message: message } %>
        </div>
    </div>
</div>